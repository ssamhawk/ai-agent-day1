# 🧪 Тестові питання для перевірки Reranking

## 📝 Інструкція по тестуванню

1. **Завантаж всі документи** через "📤 Manage Documents"
2. **Відкрий Options** та налаштуй:
   - Top chunks: 5
   - Min similarity: 0.0
   - Temperature: 0.7
3. **Увімкни Reranking**:
   - ✅ Поставь галочку "🔄 Enable Reranking"
   - Retrieve chunks: 20
   - Final chunks: 5
4. **Натисни "🔄 Compare Reranking"** для кожного питання

---

## 🎯 Питання для тестування

### Питання 1: Специфічна команда Docker
**Питання:** `How do I stop a running Docker container?`

**Очікуваний результат:**
- Без reranking: може показати загальну інформацію про Docker
- З reranking: має точно знайти секцію "Basic Docker Commands" з командою `docker stop`

---

### Питання 2: Kubernetes концепції
**Питання:** `What is a Pod in Kubernetes and how is it different from a container?`

**Очікуваний результат:**
- Без reranking: може змішати інформацію з різних файлів
- З reranking: має чітко знайти розділ "Pods" з kubernetes_intro.md

---

### Питання 3: Python async patterns
**Питання:** `Show me how to handle timeout in Python async code`

**Очікуваний результат:**
- Без reranking: може показати загальну інформацію про async
- З reranking: має знайти конкретний приклад з `asyncio.wait_for()`

---

### Питання 4: Machine Learning типи
**Питання:** `What are the main types of machine learning and when should I use each?`

**Очікуваний результат:**
- Без reranking: може показати тільки supervised learning
- З reranking: має показати всі три типи (supervised, unsupervised, reinforcement)

---

### Питання 5: REST API методи (ГАРНИЙ ПРИКЛАД!)
**Питання:** `What's the difference between PUT and PATCH in REST API?`

**Очікуваний результат:**
- Без reranking: kubernetes_intro.md на 3-му місці (низька релевантність)
- З reranking: docker_basics.md піднімається з 6-го місця на 2-ге (rank_change: +4)
- **ВАЖЛИВО:** Тут добре видно як reranking змінює порядок документів!

**Що звернути увагу:**
- Similarity score може бути низьким (0.11), але rerank score покаже справжню релевантність
- Rank change показує наскільки позиції змінилися після reranking

---

### Питання 6: Cross-domain (складне)
**Питання:** `How to containerize a Python async application with proper error handling?`

**Очікуваний результат:**
- Без reranking: може показати нерелевантні chunk'и
- З reranking: має знайти інформацію про Docker + Python async з різних файлів

---

### Питання 7: Конкретний приклад коду
**Питання:** `Show me an example of Docker Compose configuration`

**Очікуваний результат:**
- Без reranking: може показати загальну інформацію
- З reranking: має знайти конкретний YAML приклад з docker-compose.yml

---

### Питання 8: Концептуальне питання
**Питання:** `When should I NOT use async programming in Python?`

**Очікуваний результат:**
- Без reranking: може показати переваги async
- З reranking: має знайти розділ "When to Use Async" з негативними кейсами

---

### Питання 9: Складний cross-domain (ДУЖЕ ХОРОШЕ!) ⭐
**Питання:** `How do I run tests automatically when code is pushed to GitHub?`

**Очікуваний результат:**
- **Без reranking:** Може показати testing_strategies.md (схожі слова "tests", "run")
- **З reranking:** Має знайти github_actions.md з конкретним прикладом CI/CD workflow!

**Чому це хороший приклад:**
- Embedding бачить "tests" + "run" → думає про testing
- Cross-Encoder розуміє контекст "GitHub" + "automatically when pushed" → CI/CD!
- Це демонструє силу reranking: розуміння НАМІРІВ, а не просто слів

---

### Питання 10: Специфічний технічний запит (ВІДМІННЕ!) ⭐⭐
**Питання:** `Why are my database queries slow even with small tables?`

**Очікуваний результат:**
- **Без reranking:** Може знайти загальну інформацію про databases або performance
- **З reranking:** Має знайти database_indexing.md з розділом "When to Use Indexes" та "Query Optimization"

**Чому це чудовий приклад:**
- Питання не містить слово "index" або "optimization" явно
- Embedding може не зрозуміти проблему
- Cross-Encoder розпізнає ПРОБЛЕМУ та знайде РІШЕННЯ
- Покаже як reranking розуміє контекст та intent!

---

### Питання 11: Порівняння технологій (EXCELLENT!) ⭐⭐⭐
**Питання:** `Should I use GitHub Actions or Jenkins for my CI/CD pipeline?`

**Очікуваний результат:**
- **Без reranking:** Може знайти Docker або Kubernetes (схожий DevOps контекст)
- **З reranking:** Має знайти github_actions.md з розділом "GitHub Actions vs Jenkins"!

**Чому це НАЙКРАЩИЙ приклад:**
- Embedding бачить "pipeline" → може думати про Docker/containers
- Питання містить ПОРІВНЯННЯ двох інструментів
- Cross-Encoder розуміє що це питання вибору між конкретними інструментами
- Має знайти ТОЧНИЙ розділ з порівнянням!

---

### Питання 12: Негативний кейс (anti-pattern)
**Питання:** `What are common mistakes in writing tests?`

**Очікуваний результат:**
- **Без reranking:** Може показати позитивні практики тестування
- **З reranking:** Має знайти розділ "Common Testing Mistakes" з конкретними помилками

**Чому важливе:**
- Показує що reranking розуміє різницю між "how to do" vs "what NOT to do"
- Embedding часто плутає позитивні та негативні контексти

---

## 🔍 На що звертати увагу:

### Similarity Score (Embedding)
- Базується на семантичній схожості слів
- Може бути високим навіть для неточних результатів

### Rerank Score (Cross-Encoder)
- Більш точна оцінка релевантності
- Враховує контекст запиту та документа разом

### Rank Changes (⬆️⬇️➡️)
- **⬆️ Зелений**: Документ піднявся (став більш релевантним)
- **⬇️ Червоний**: Документ опустився (став менш релевантним)
- **➡️ Сірий**: Позиція не змінилася

---

## 📊 Очікувані покращення:

1. **Більш точні відповіді** на специфічні питання
2. **Краще ранжування** релевантних документів
3. **Менше "шуму"** від семантично схожих, але неточних результатів
4. **Кращий пошук** конкретних прикладів коду та команд

---

## 🎓 Висновок тесту:

Після тестування ти маєш побачити, що:
- Reranking покращує якість відповідей на **специфічні** питання
- Cross-Encoder краще розуміє **контекст** та **intent** запиту
- Деякі документи **різко змінюють** свою позицію після reranking
- Відповіді стають більш **точними** та **релевантними**

---

## ⚠️ ВАЖЛИВО: Чому відповіді можуть бути схожими?

**Це нормально!** Якщо відповіді LLM схожі, це НЕ означає, що reranking не працює.

### Причини схожості відповідей:

1. **Перший документ однаковий**
   - Якщо найкращий документ однаковий в обох випадках, LLM згенерує схожу відповідь
   - LLM в основному базується на перших 1-2 документах

2. **Різниця в chunks, не в тексті**
   - Reranking змінює **порядок документів** та **які документи потрапляють в топ**
   - Але якщо основна інформація є в обох випадках, відповідь буде схожою

3. **Де дивитися на різницю:**
   - ✅ **Rank Changes (⬆️⬇️)**: Показують як документи змінили позиції
   - ✅ **Rerank Score**: Показує справжню релевантність
   - ✅ **Список документів**: Може бути різний порядок або інші файли
   - ❌ **НЕ дивись тільки на текст відповіді**: Він може бути однаковим навіть якщо reranking працює

### Найкращі питання для демонстрації:
- Питання 5 (PUT vs PATCH) - показує **великі rank changes**
- Питання де embedding similarity низький, але питання специфічне
- Питання що потребують інформації з різних документів

### Приклад успішного reranking:
```
Без reranking: [doc1: 0.42, doc2: 0.34, kubernetes: 0.15]
З reranking:   [doc1: 0.42, docker: 0.11 (+4), doc2: 0.34 (-1)]
                                      ^^^
                           Docker піднявся з 6-го місця!
```

Reranking **працює**, навіть якщо відповіді схожі! Дивись на **rank changes** та **rerank scores**!
